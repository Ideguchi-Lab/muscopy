name: ruff

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ruff:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/ruff-action@v3
        with:
          version-file: requirements-dev.txt
          args: check ./muscopy ./tests ./examples

      - run: ruff format --check --diff
